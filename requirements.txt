# Pinned per Decision #11 (reproducibility). Versions below are sensible
# starting points for Python 3.13 on the RTX 3090 workstation (CUDA 13.0
# driver confirmed) — freeze exact versions with `pip freeze` once the
# environment is actually built, and keep this file in sync with it.

# Core data handling
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0  # parquet I/O for F-DATA/PM100

# Classical ML (Decision #14)
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0

# Deep learning (Decisions #12, #13) — install via the official PyTorch
# index matching CUDA 13.0 rather than plain pip, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1

# Hyperparameter tuning (Decision #5)
optuna==4.1.0

# Interpretability (Decision #9)
shap==0.46.0

# Stats (Decision #6)
scipy==1.14.1

# Plotting (Track B5)
matplotlib==3.9.3
seaborn==0.13.2

# Config
pyyaml==6.0.2

# Notebooks
jupyterlab==4.3.2
ipykernel==6.29.5
